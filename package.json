{
  "name": "resobox-core",
  "version": "1.0.0",
  "description": "A Node.js project for running Python scripts",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p build && clang++ -std=c++17 -o build/app main.cpp -march=native -lsndfile -lportaudio",
    "start-compiled": "cd build && ./app",  
    "start": "npm run build && npm run start-compiled",
    "electron": "electron ./ui/electron"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.2.1"
  }
}
